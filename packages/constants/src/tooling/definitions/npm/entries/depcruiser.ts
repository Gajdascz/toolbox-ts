import { entries } from '../factory.js';

export const DEPCRUISER = entries({
  name: 'dependency-cruiser',
  description: 'Validate and visualize dependencies.',
  repo: 'https://github.com/sverweij/dependency-cruiser'
});
