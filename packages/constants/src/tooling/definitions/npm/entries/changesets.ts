import { entries } from '../factory.js';

export const CHANGESETS = entries({
  name: '@changesets/cli',
  description:
    'A tool to manage versioning and changelogs with a focus on multi-package repositories',
  repo: 'https://github.com/changesets/changesets'
});
