import { entries } from '../factory.js';

export const TOOLBOX_TS = entries({
  name: '@toolbox-ts/test-utils',
  description: 'Memory-safe utilities for testing fs operations.',
  repo: 'https://github.com/gajdascz/toolbox-ts/tree/main/packages/test-utils'
});
