name: Push on Any Non-Main Branch

on:
  push:
    branches-ignore:
      - main

jobs:
  validate-push:
    uses: ./.github/workflows/validate.yml
    with:
      changeset: false
      commitlint_mode: 'last'
