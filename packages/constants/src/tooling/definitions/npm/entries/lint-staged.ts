import { entries } from '../factory.js';
export const LINT_STAGED = entries({
  name: 'lint-staged',
  repo: 'https://github.com/lint-staged/lint-staged',
  description: 'Run tasks against staged git files.'
});
