import { entries } from '../factory.js';

export const RIMRAF = entries({
  name: 'rimraf',
  repo: 'https://github.com/isaacs/rimraf',
  description: 'The UNIX command rm -rf for node in a cross-platform implementation.'
});
